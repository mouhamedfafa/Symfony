
{% extends 'base.html.twig' %}

{% block title %}Hello ClasseController!{% endblock %}

{% block body %}

<div class="    ">
    <div class="card-body">
        <h4 class="card-title">Liste des Ac</h4>
        <p class="card-text">
        <table class="table table-striped table-hover">
            <thead class="table table-dark">
                <tr class="table-active"> 
                <th scope="col">Nom Complet</th>
                <th scope="col">Adresse</th>
                <th scope="col"></th>
        <th scope="col">  </th>
                <th scope="col">  </th>
                </tr>
            </thead>
            <tbody>
       {% for acs in ac %}
        
             <tr>
             <td>{{acs.nomComplet}}</td>
             <td>{{acs.adresse}}</td>
             <td><a href="{{ path('edd_ac', {id:acs.id}) }}"><button class='btn btn-outline-success'>editer</button></a>&nbsp<button type='button' class='btn btn-outline-danger'>supprimer</button> <td>

            </tr>
{% endfor %}
        
    
            </tbody>
        </table>

        </p>
      <div class="d-flex" justify-content-center style="margin-left:50rem" >
        {%  do ac.setPageRange(10) %}
               {{ knp_pagination_render(ac,'pagination.html.twig') }}
        </div>
    </div>  
</div>

  


{% endblock %}


