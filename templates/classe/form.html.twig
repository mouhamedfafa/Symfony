


{% extends 'base.html.twig' %}
 
{% block body %}
 
    {{ form(form) }}
 
    {# {{ form_row(form.libelle,{class}) }}
    {{ form_row(form.filiere) }}
    {{ form_row(form.niveau) }} #}
    {# {{ form_row(form.ac.getNomComplet())}} #}

   
 
    {# {{ form_end(form) }} #}
 
{% endblock %}